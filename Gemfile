# frozen_string_literal: true

source 'https://rubygems.org'

gem 'temporalio'

# Some samples require certain dependencies, so they are in groups below alphabetically by group
gem 'opentelemetry-api', group: :opentelemetry
gem 'opentelemetry-exporter-otlp', group: :opentelemetry
gem 'opentelemetry-sdk', group: :opentelemetry

group :development do
  gem 'minitest'
  gem 'rake'
  gem 'rubocop'
end
